<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_splash_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/Black"
    android:orientation="vertical"
    tools:context="xchg.online.studenttrack.SplashScreenActivity">
    <ImageView
        android:id="@+id/xchg_logo"
        android:layout_width="wrap_content"
        android:layout_centerInParent="true"
        android:layout_height="wrap_content"
        android:src="@drawable/track_logo"
        />
    <ProgressBar
        android:theme="@style/Base.Widget.AppCompat.ProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/splash_loading_pb"
        android:layout_marginBottom="@dimen/splash_progressbar_marginbot"
        android:layout_centerHorizontal="true"
        android:indeterminate="true"
        style="@style/SplashScreenProgressBarStyle"
        android:layout_alignParentBottom="true" />

    <TextView
        android:id="@+id/annotation_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/xchg_logo"
        android:layout_centerHorizontal="true"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/white"/>
</RelativeLayout>
